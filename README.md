# DarkGhostwriter
Stylish CSS for Ghostwriter by SpecterOps - Dark Mode for Ghostwriter!


Just some simple drop in Stylish CSS!

Create a new style
Set your url prefix scope at the top of the sheet, just replace the below and go!
  ```
    @-moz-document url-prefix("https://<ghostwriter_host/ip>")

  ```
